# Server-side only — used by src/app/api/analyze-meal/route.ts to estimate
# meal macros from a text description. Never exposed to the browser.
# Get a key at https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=
